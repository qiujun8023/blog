<% if (post.tags && post.tags.length){ %>
  <div class="article-tag tagcloud iconfont icon-05tag">
    <%- list_tags(post.tags, {
      show_count: false,
      class: 'article-tag'
    }) %>
  </div>
<% } %>